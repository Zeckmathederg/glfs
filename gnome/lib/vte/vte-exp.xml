<sect2>
<title>Command explanations</title>

<para><userinput>--libexecdir=/usr/sbin</userinput> : This switch puts
libexec files in <filename>/usr/sbin</filename> instead of
<filename>/usr/libexec</filename>.</para>

<para><userinput>--disable-gtk-doc</userinput> : This switch prevents the
building of documentation as the directories are currently empty and
will cause errors in the make command if this switch is missing.</para>

</sect2>

